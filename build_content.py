from pathlib import Path
import json
R=Path(__file__).parent
fields=[
('SchemaVersion','DInt','1','Export layout version; must be 1'),('Heartbeat','DInt','0','Changes while the monitor executes; not proof of robot communication'),('MonitorState','DInt','0','0 unqualified, 1 waiting/between, 2 cycle active'),('CurrentShift','DInt','0','0 gap or invalid clock; 1 day; 2 night'),('CyclesThisHour','DInt','0','Completed cycles in the current calendar hour'),('CyclesPreviousHour','DInt','0','Last observed closed hour; use its key and partial flag'),('Shift1Current','DInt','0','Current/latest day-shift running count'),('Shift1Previous','DInt','0','Most recently closed day-shift count'),('Shift2Current','DInt','0','Current/latest night-shift running count'),('Shift2Previous','DInt','0','Most recently closed night-shift count'),('PreviousCycleSeconds','Real','0.0','Held duration of the last valid completed cycle'),('BetweenCycleSeconds','Real','0.0','Live waiting duration after a known completion; check BetweenActive'),('PreviousBetweenSeconds','Real','0.0','Held waiting duration captured at the next accepted start'),('CycleSeconds','Real','0.0','Live active-cycle duration, diagnostic'),('ThisHourKey','DInt','-1','Days since 1990-01-01 multiplied by 24, plus local hour'),('PreviousHourKey','DInt','-1','Identity of the held hour; -1 means no held hour'),('Shift1Day','DInt','-1','Day-shift start date, days since 1990-01-01'),('Shift1PreviousDay','DInt','-1','Held day-shift start date; -1 means none'),('Shift2Day','DInt','-1','Night-shift START date, including after midnight'),('Shift2PreviousDay','DInt','-1','Held night-shift start date; -1 means none'),('PreviousCycleValid','DInt','0','1 once a valid completion has populated the held cycle'),('PreviousBetweenValid','DInt','0','1 once a known interval has been captured'),('ThisHourPartial','DInt','1','1 means this hour is incomplete or uncertain'),('PreviousHourPartial','DInt','1','1 means held hour is not a complete trusted hour'),('Shift1Partial','DInt','1','Day-shift current completeness flag'),('Shift1PreviousPartial','DInt','1','Held day-shift completeness flag'),('Shift2Partial','DInt','1','Night-shift current completeness flag'),('Shift2PreviousPartial','DInt','1','Held night-shift completeness flag'),('SignalsValid','DInt','0','Input/robot communication qualification supplied by site logic'),('ClockValid','DInt','0','Valid local calendar supplied by clock adapter'),('CycleActive','DInt','0','1 while a qualified cycle is being observed'),('BetweenActive','DInt','0','1 while live between-cycle duration is known'),('TimingFault','DInt','0','Latched restart/tick discontinuity or elapsed-counter saturation'),('TotalCompleted','DInt','0','All accepted completed cycles, including gap/clock-unassigned'),('GapCompleted','DInt','0','Completions in 06:00–06:30 gap'),('ClockUnassigned','DInt','0','Completions while local clock unavailable'),('ClockReview','DInt','0','Latched invalid clock/discontinuity; review period completeness'),('CounterSaturated','DInt','0','A reporting counter reached DINT maximum; never wraps'),('ObserverEnable','DInt','0','Commissioning enable: default 0; ONLY set this new DB field to 1 after review')]
F=[dict(name=n,type=t,initial=i,offset=k*4,description=d,kepware='Float' if t=='Real' else 'Long') for k,(n,t,i,d) in enumerate(fields)]
(R/'schema.json').write_text(json.dumps(F,indent=2))
# Public help links. Installed-version F1 help and the actual load preview remain authoritative.
sources=[
('siemens-run','Siemens: downloading changes in RUN','https://docs.tia.siemens.cloud/r/en-us/v20/compiling-and-downloading-plc-programs/downloading-blocks-for-s7-300/400-s7-300-s7-400/downloading-blocks-in-the-run-operating-state-to-the-device-s7-300-s7-400'),
('siemens-source','Siemens: external SCL source files','https://docs.tia.siemens.cloud/r/en-us/v20/creating-and-managing-blocks/using-external-source-files-for-stl-and-scl/basics-of-using-external-source-files'),
('siemens-local','Siemens: RD_LOC_T local calendar','https://docs.tia.siemens.cloud/r/en-us/v20/extended-instructions-s7-1200-s7-1500/date-and-time-of-day-s7-1200-s7-1500/time-of-day-functions-s7-1200-s7-1500/rd_loc_t-read-local-time-s7-1200-s7-1500'),
('siemens-tick','Siemens: TIME_TCK elapsed counter','https://docs.tia.siemens.cloud/r/en-us/v20/extended-instructions-s7-1200-s7-1500/date-and-time-of-day-s7-1200-s7-1500/time-of-day-functions-s7-1200-s7-1500/time_tck-read-time-counter-s7-1500'),
('kepware-driver','PTC: Siemens TCP/IP Ethernet driver','https://support.ptc.com/help/kepware/drivers/en/kepware/drivers/SIEMENSTCPIPETHERNET/HelpContents.html'),
('kepware-address','PTC: DB address syntax and data types','https://support.ptc.com/help/kepware/drivers/en/kepware/drivers/SIEMENSTCPIPETHERNET/Standard_S7_300_400_Item_Syntax.html'),
('kepware-csv','PTC: CSV import/export','https://support.ptc.com/help/kepware/kepware_server/en/kepware/server/csv-import-export.html')]
I=[]
def topic(id,title,summary,html): I.append(dict(id=id,title=title,summary=summary,html=html))
def steps(*s):return '<ol>'+''.join('<li>'+x+'</li>' for x in s)+'</ol>'
def note(s):return '<div class="callout">'+s+'</div>'
def pre(s):
 import html
 return '<pre><code>'+html.escape(s)+'</code></pre>'
topic('first','Start here: scope and safe sequence','Back up, verify one machine, then build and commission only the reporting addition.',
note('<strong>This is a commissioning guide, not a PLC connection.</strong> This page never connects to, downloads to, or writes to a machine. The SCL files are reference implementations that have not been compiled in TIA or tested on these machines.')+
steps('Start with MM4. Finish its online checks, compile review and observed-cycle test before repeating on MM5. Treat MM6 as a different controller and signal mapping.','Obtain the site\'s approval for a software change and an operator available to hold the machine in a normal safe idle condition. CPU RUN is not permission to alter a moving machine.','Use separate working copies and timestamped archives of the correct current project for every machine. Save a backup of the Kepware project too.','Keep changes in new standard reporting blocks. Add only their calls and a startup-reset assignment to existing cyclic/startup OBs. Do not edit F-programs, valves, safety permissives, IPs or PROFINET names.','Use the stop gates in each checklist. A checked box records your confirmation, not an automated validation. Unsupported clock calls, unresolved signals, block-number conflicts or an unexpected STOP request mean pause that machine\'s commissioning.')+
'<p>MM4/MM5 are based on the provided V15 PF1000723 archive. MM6 is based on the V16 PF1000825 archive. The files are an offline snapshot, not proof of what is running today. This public app includes only the needed signal notes and new reference code, not the customer project archives.</p>')
topic('metrics','Exactly what the numbers mean','Completed-cycle counts, local clock boundaries and held values.',
'<table><thead><tr><th>Value</th><th>Rule used here</th></tr></thead><tbody>'+''.join('<tr><td>'+a+'</td><td>'+b+'</td></tr>' for a,b in [
('Cycle','Start on a qualified rising close-command edge. Finish after robot permission has been FALSE during that cycle and then returns TRUE.'),
('Previous cycle','Copy live duration only at a valid completion. Keep the old held value during the next cycle or an abort.'),
('Between cycles','Start at a valid completion. Capture at the next accepted start. Remain live while idle even if automatic mode is off, provided the interval remains known.'),
('Current/previous hour','Use the PLC local date + hour. Roll the old count first, then credit a completion to the new hour. It is not a rolling last-60-minutes rate.'),
('Shift 1','06:30 inclusive to 17:00 exclusive.'),('Shift 2','17:00 inclusive to 06:00 exclusive next day. Its identity is the date on which 17:00 occurred.'),('Unassigned gap','06:00 inclusive to 06:30 exclusive. Count in the hour and total, but neither shift; keep a separate gap count.'),('Count ownership','Count completed cycles, not starts. A cycle crossing a boundary belongs to the hour and shift in which it finishes.'),('Held shift counts','Shift1Previous and Shift2Previous each store their latest closed shift. Current count resets when that shift\'s next dated instance starts, not at midnight.'),('Validity','A zero without a valid period key may mean no history yet. Keep partial/valid flags and keys available in Kepware; do not label a partial first hour as a complete hour.')])+'</tbody></table>'+note('<strong>Between-cycle time is not a universal downtime timer.</strong> A stop during an unfinished cycle belongs to that active cycle. An abort or lost robot communication makes the interval uncertain. CPU STOP or power loss cannot be timed by scan-based logic. Use external historian/staleness detection for off-CPU downtime.'))
topic('network','Connect the laptop and confirm the correct PLC','Reachability, network adapter and online identity, without changing device addresses.',
steps('Ask for the approved engineering connection and the current IP/CPU identification for MM4, MM5 and MM6. Enter confirmed values in each machine\'s local notes. Duplicate projects do not imply duplicate IPs.','Connect your laptop\'s Ethernet adapter to the approved switch/port. Use the assigned unused laptop IP/subnet or approved DHCP configuration. Do not borrow a PLC address. A conference-room socket may be on a different VLAN even in the same building.','Open the correct TIA version and working project. Switch to Project view. Expand the PC station / PLC device until you see <strong>Program blocks</strong>. Select the CPU, not the HMI or robot.','Choose <strong>Go online</strong>. In the connection dialog use <strong>PN/IE</strong>, select the actual wired adapter, and choose the known PLC interface/path. If discovery is empty, check routing/subnet and use the known address; discovery and routed IP communication are different.','Under <strong>Online & diagnostics</strong>, record the device identity, order number, firmware and operating state. Cross-check it against the machine/operator. Do not rely only on a project name or an orange icon.','Run the online/offline comparison for Program blocks. Resolve differences before adding anything. Preserve the commented source project; an upload may not recreate original SCL. Keep the upload/backup separate instead of overwriting it.','From the Kepware PC, independently confirm that its approved network route reaches the same controller endpoint. For the normal S7 TCP driver, have IT check TCP port 102. Do not disable the firewall or bridge networks as a shortcut.')+'<p>Do not set rack/slot to a remembered default. WinAC PC-station routing and the MM6 software controller must be checked against their configured connection and driver documentation. Use an existing working device connection where possible.</p>')
topic('find-code','Find OB1 and the two event bits','Program blocks, cross-references and a read-only watch table.',
steps('Expand <strong>PLC → Program blocks</strong>. Find the organization block with number <strong>OB1</strong>; its visible name may be “Cycle Execution” or another language. Double-click it. OB1 is the main cyclic program, not an ordinary subroutine to call.','Open the machine section below for its address candidates. Search/cross-reference a tag or absolute address to find where it is written, then follow the existing FC200/FC166/FC452 paths. A read reference by itself does not prove the signal\'s producer.','Under <strong>Watch and force tables</strong>, choose <strong>Add new watch table</strong>. Add the start DB bit, output, end DB bit, raw input and configuration selectors. Use <strong>Monitor all</strong> / the glasses icon. Leave modify/force columns unused.','Observe normal cycles with the operator. The close edge should match the command you intend to time. The end permission must go low and later return high after clearing the pins. A slowly refreshed watch table can miss short transitions; use an approved trace or a temporary read-only capture in the new observer if needed.','Identify existing production/automatic, robot communication-valid and abort signals by cross-reference and observation. Record their exact meanings. Do not tie SignalsValid or ProductionMode permanently TRUE just to obtain counts.','Inspect the end of OB1. Both archived programs have 48 networks. Add the new calls <em>after</em> existing network 48, provided that location actually executes every normal scan. On MM6 it follows RobotMain. Live network numbering may differ.')+
note('Never write the candidate motion/permission bits. They are read-only inputs to your new monitor. The close signal is a command, not a sensor proving movement. On MM6, do not invert RobotInMouldNotClear merely because its name sounds backward.'))
topic('blocks','Siemens blocks and tags, without the shorthand','FB = your routine with memory; instance DB = its private state; global DB = readable reporting values.',
'<p>A <strong>BOOL</strong> is one on/off bit. A <strong>DINT</strong> is a signed 32-bit integer for counts. A <strong>REAL</strong> is a floating-point value, used here for seconds. A <strong>TIME</strong> value is not the same thing as a REAL number of seconds.</p>'+steps(
'The new <strong>CSI_CycleMonitor</strong> FB contains the sequence and remembers state across scans. Its <strong>instance DB</strong> is created when you insert its call. Do not use an FC with TEMP edge-history bits; TEMP is not persistent scan-to-scan memory.',
'<strong>CSI_Report</strong> is a separate global DB containing the flat reporting fields. Kepware reads this DB, not private offsets buried inside a timer or an instance DB.',
'<strong>CSI_IO</strong> holds the new monitor\'s normalized Boolean inputs, clock outputs and StartupPending. CSI_Clock reads time into that DB; it never sets the PLC clock.',
'Before creating/importing anything, search all blocks and tags for <strong>CSI_</strong> names. Verify they are absent. Let TIA assign free numbers or choose verified free numbers. Record the actual FB, instance DB, report DB and helper block numbers per machine.',
'For the new MM6 reporting DB, use <strong>standard / non-optimized block access</strong> if the chosen Kepware driver uses absolute byte addresses. Leave every existing DB\'s layout alone. A symbolic driver may use a different supported path.',
'After compiling CSI_Report, inspect its <strong>Offset</strong> column. The provided layout deliberately uses only four-byte DINT/REAL fields. Confirm offsets before creating Kepware tags. A later inserted field changes following offsets, so append/version future fields rather than inserting them.')+
'<p>For a completely manual block: double-click <strong>Add new block</strong>, choose <strong>Function block</strong>, language <strong>SCL</strong>, enter an unused name and open it. The interface grid above the editor declares Input, Output, InOut, Static and Temp variables. The body below contains executable statements.</p>')
topic('scl','Create the reporting code and save it','Use the included source route, or enter an FB interface and body by hand.',
steps('Download <strong>Core reference (.scl)</strong> plus the correct clock adapter from Resources. Core defines the new report DB, IO DB, calendar helper and monitor FB. Use the legacy clock adapter only after confirming WinLC/WinAC instruction support; use the 1500 adapter for the archived MM6 CPU type.','In the PLC tree, expand <strong>External sources</strong>. Choose <strong>Add new external file</strong>, select the core .scl, then the one appropriate clock .scl. This is under the CPU, not the HMI.','Right-click the core source → <strong>Generate blocks from source</strong>. Generate the clock source next. If TIA warns it will overwrite an existing block, cancel and resolve the name/number collision. Do not generate anything named OB1 or OB100 from a downloaded example.','Compile the generated blocks. Open the Info/Compile results pane and double-click each error. Use F1 on READ_CLK, RD_LOC_T, TIME_TCK and conversions for the installed target. A source that has not compiled successfully must not be downloaded.','Open CSI_Report, compare the fields/offsets with the address map, and confirm <strong>ObserverEnable starts at 0</strong>. Configure the required retention only for the new reporting/state data, according to the actual CPU\'s supported behavior.','In a new final OB1 network, call CSI_Clock, map the verified read-only machine signals to CSI_IO, then call CSI_CycleMonitor once. Let TIA create a new single-instance DB. Place the StartupPending clear after that call. Use the call recipe in the next instruction.','Use <strong>Ctrl+S</strong> or Project → Save, then compile software changes. Saving changes the project on your laptop; it does not download them. Archive the reviewed working copy before commissioning.')+
note('A full external source contains FUNCTION_BLOCK, VAR and END_FUNCTION_BLOCK declarations. Do not paste the whole file into the body-only SCL editor. Use External sources, or declare the interface in the grid and paste only the body. Source import can overwrite identically named blocks. <a href="#siemens-source">Siemens source-file reference</a>.'))
topic('call','Wire the new call and the startup reset','One cyclic call, safe default disabled, and no old DB rewritten.',
'<p>Use a separate final network for each of these actions if that is easier to inspect. In LAD, drag the new blocks from Program blocks into the network and fill the pins. In an STL OB, insert a CALL through the editor so TIA builds the formal parameter list; do not guess interface declarations.</p>'+steps(
'Call <strong>CSI_Clock</strong> once per scan before the monitor. It supplies TickMs, ClockOK, DaySerial, MinuteOfDay and Second in CSI_IO.',
'Assign CSI_IO.CloseCommand from the verified start bit and CSI_IO.RobotPermit from the verified end bit. Assign ProductionMode, SignalsValid and AbortCycle from the observed, appropriate machine conditions. These assignments only write the <em>new CSI_IO DB</em>.',
'Use a DINT equality comparison of CSI_Report.ObserverEnable to 1 and write its Boolean result into CSI_IO.EnableRun. Call CSI_CycleMonitor with Enable := CSI_IO.EnableRun; map the other pins to same-named CSI_IO members. Restart maps to CSI_IO.StartupPending. This is data qualification, not a conditional call: keep the FB executing even when disabled.',
'Immediately after the monitor call, set <strong>CSI_IO.StartupPending := FALSE</strong>. On first download its start value is TRUE, so the monitor seeds input history instead of counting an already-high input.',
'In the existing appropriate startup OB, append <strong>CSI_IO.StartupPending := TRUE</strong> to the standard program. Usually this is OB100; verify all startup modes supported/configured on this CPU. If startup OB101/102 are actually used, handle those paths too. Do not replace existing startup code or create competing startup logic blindly.',
'Keep ObserverEnable = 0 until the physical signal trace, mapping, compile and download review are complete. Then, with approval, modify <em>only this new commissioning field</em> to 1 using a normal DB/watch modification, not force. Kepware remains read-only.')+
pre('Example mapping only, for a VERIFIED Servo MM4/MM5:\n"CSI_IO".CloseCommand := %DB98.DBX112.7;\n"CSI_IO".RobotPermit := %DB94.DBX29.4;\n\nMM6 uses %DB94.DBX29.6 for RobotPermit.\nDo not copy MM4/MM5\'s end address to MM6.\n\nRemaining mappings must be filled from verified machine logic.\nNo automatic-mode / communication / abort addresses are guessed.')+
'<p>Use TIA\'s accepted symbolic member names or absolute operand format for the actual target. The mapping fragment is not a standalone block. In LAD, use the existing bit as a contact and a coil addressed only to the corresponding CSI_IO member.</p>')
topic('ladder','How the timers, MOVE and count logic work','Ladder equivalents for understanding the SCL reference.',
'<p>The reference uses elapsed milliseconds from a monotonic PLC counter, not a timer that resets every time its input drops. This avoids changing cycle duration when someone adjusts the calendar clock.</p>'+steps(
'<strong>Rising edge:</strong> StartPulse := CloseCommand AND NOT PreviousClose; then update PreviousClose at the end of each scan. In LAD this is a positive-edge contact / R_TRIG with persistent memory. Seed the memory at startup so an input already high does not count as a new event.',
'<strong>Cycle active:</strong> accept the start only while armed, enabled, in production and with valid signals. Latch cycle active. Ignore repeat close edges until this cycle completes or aborts.',
'<strong>Elapsed time:</strong> add the valid tick difference to active elapsed milliseconds. On completion, <strong>MOVE</strong> / SCL assignment the finished value to the held field, then clear the live value. In SCL, <code>PreviousCycleSeconds := DINT_TO_REAL(CycleMs) / 1000.0;</code> is the value-copy operation.',
'<strong>End pulse:</strong> require permission to have gone low during the active cycle, then accept its next rising edge. The end pulse happens once in the PLC, even if Kepware polls much more slowly.',
'<strong>Counts:</strong> add 1 to DINT counts on the completion pulse. In LAD use an ADD under that pulse, or a properly sized counter. Do not increment every scan while the permit remains high, and do not ask Kepware to count a one-scan pulse.',
'<strong>Periods:</strong> compare date-plus-hour key, not just the hour number. MOVE the old count and its key/quality into held storage, clear the current count and set its new key. Do the same for each dated shift instance; perform rollover before incrementing a boundary completion.')+
'<p>Elapsed storage in this reference saturates at 2,147,483,647 milliseconds (about 24.8 days); TimingFault flags a saturated or discontinuous interval. Treat such a held duration as limited, not an exact measurement. A basic IEC TON can explain IN/PT/Q/ET: IN enables it, PT is the preset and ET is elapsed TIME. But a normal TON resets when IN is false and caps at its preset. Copy ET before resetting it and account for the preset limit if choosing that approach. Do not substitute an arbitrary T-number or small S5 timer for this reporting design.</p>')
topic('clock','Read PLC time: two controller paths','Calendar for shifts; elapsed counter for durations; no clock writes.',
'<p><strong>MM4/MM5:</strong> verify the WinLC/WinAC target and its installed instruction support. The legacy adapter reads READ_CLK into an eight-byte DATE_AND_TIME, checks its status and decodes the BCD calendar. That value does not have DTL.YEAR or DTL.HOUR members. Confirm whether this CPU clock is already maintained in plant local time.</p>'+ 
'<p><strong>MM6:</strong> the archive identifies CPU 1507S F, firmware V2.5. The 1500 adapter reads RD_LOC_T into DTL. Return codes 0 and 1 are accepted; 1 indicates daylight-saving time. Confirm its configured time zone and displayed local date/time against the plant schedule. <a href="#siemens-local">Siemens local-time reference</a>.</p>'+ 
'<p>Both adapters use TIME_TCK for duration deltas. The monitor handles its normal wrap. This counter stops in CPU STOP and resets on a warm restart; the startup guard deliberately invalidates an unfinished interval. It does not invent elapsed time while the CPU is off. <a href="#siemens-tick">Siemens counter reference</a>.</p>'+ 
note('Test 06:00, 06:30, 17:00, midnight, date rollover and daylight-saving/clock corrections in an isolated test instance or simulation. Do not change a running production PLC\'s clock just to test counters. This reference marks uncertain periods partial; a backwards local clock movement pauses calendar assignment until local time passes the prior high-water mark, avoiding overwriting an earlier hour. Those completions remain in TotalCompleted and ClockUnassigned.')+
'<p>The first observed hour/shift is partial unless the monitor continuously observed its actual boundary. A lost clock, skipped boundary, restart, disabled monitoring or lost signal qualification also marks affected periods partial. Check the period keys; the last observed held hour is not necessarily the immediately preceding wall-clock hour after an outage.</p>')
topic('download','Can you change it while the CPU stays in RUN?','Often possible for a limited change, but the load preview decides.',
steps('Finish the edit in the offline project while connected online for comparison. Siemens does not require rewriting the whole program to change a block. Editing/saving alone does not change the CPU.',
'Coordinate a safe commissioning window and compare the current CPU program with your working project. Select the intended additions and changed OB calls, then use <strong>Download to device → Software (only changes)</strong>.',
'Read <strong>Load preview</strong> before committing. Verify the target identity and every affected block. Review dependencies, DB initialization, requested operating-state changes, deletions and F-related changes.',
'Cancel for any unexpected STOP, hardware download, broad reinitialization, deletion, safety-program change or source mismatch. Use a planned shutdown when the change cannot be made within the approved window.',
'Only proceed when the preview and operating conditions are acceptable to the site. Do not restart a machine automatically because a dialog offers to do so.',
'After the change, compare online/offline, check diagnostics/scan time, and keep the new observer disabled until its inputs and stored values are verified. Subsequent FB interface/DB layout changes need another full impact review.')+
'<p>RUN download is not a guarantee of uninterrupted or risk-free production. See <a href="#siemens-run">Siemens\' RUN-download reference</a>. Its online manual is a newer version; use V15/V16 target F1 help and your actual preview for supported behavior.</p>')
topic('retention','Keep held values and handle restarts','Retention must be verified; a source start value is not a saved actual value.',
steps('Decide with the site which reporting counts and held durations must survive warm restart/power loss. Configure retention for the new report DB and required instance state using the actual CPU\'s properties. Capacity and behavior differ between legacy WinAC and the 1500.',
'Keep held times and their valid flags together. Keep counts, period keys and partial flags together. Never retain a count while discarding the key that identifies its period.',
'The monitor discards an unfinished active interval after startup and seeds the input history. It preserves retained held values and marks interrupted periods partial. It cannot recover cycles completed while not executing.',
'Before a later download that may initialize a new DB, export/record its online actual reporting values. Re-importing the source or loading DB start values can replace counts; retention does not protect against every download/reset.',
'Only test restart/power-loss behavior in an approved shutdown/test window. Record what persists and confirm the observer does not create a completion from startup-high signals.')+
'<p>TimingFault and ClockReview are diagnostic latches, not machine interlocks. The reference does not add an automatic history reset. Counter saturation is flagged instead of allowing a signed count to wrap negative.</p>')
topic('kepware','Kepware: from PLC DB to readable server tags','Use the installed driver and one read-only test tag before building the list.',
steps('At the Kepware PC, open the actual server configuration application and save/export a backup of its active project. Record the product version and licensed drivers. Confirm authorization before applying server changes; configuration edits can affect connected clients.',
'Inspect existing channels/devices for these PLCs. Prefer an already working device connection. For a new conventional S7 polling connection, select <strong>Siemens TCP/IP Ethernet</strong>. Do not accidentally pick the Unsolicited/Server driver, which has a different communication role.',
'If the site instead uses Siemens S7 Plus symbolic access or an approved OPC UA connection, retain that architecture and use its driver\'s addressing. The generated absolute map in this app is specifically for the normal Siemens TCP/IP Ethernet driver.',
'Create or verify a separate device named MM4, MM5 or MM6 under an appropriate Siemens channel. Enter its confirmed IP/interface and controller model. Confirm rack/slot/TSAP for this actual controller; do not copy an arbitrary S7-300 slot or a robot IP.',
'For MM6 absolute DB reads, have the site review the CPU\'s permitted communication method and protection settings. If legacy S7 reads are blocked, do not disable security indiscriminately or change hardware settings while running. Use an approved supported driver/access method or schedule the required configuration change.',
'Under the device, create a tag group such as <strong>Production</strong>. Create <strong>SchemaVersion</strong> first, using the actual report DB number and DINT0 address. Set data type Long and client access Read Only. A suggested initial scan rate is 1000 ms; confirm the installed server\'s scan mode/client overrides.',
'Open the OPC Quick Client or the site\'s approved test client. Confirm Good quality and value 1, then add Heartbeat and verify it changes while the observer executes. This tests the PLC reporting path, not robot communication validity.',
'Add one REAL held-time tag and compare it with the TIA watch value. Then add the remaining counts, time values and validity/partial fields from the machine address map.',
'For bulk import, create/export one real tag from this installed Kepware version and use that CSV header/template. The app\'s downloadable CSV is an <strong>address reference</strong>, not a guaranteed version-specific import file. Quote addresses containing a comma.',
'Use the client item path, for example <code>Siemens.MM4.Production.PreviousCycleSeconds</code>, in the dashboard/historian. Channel and device names determine the real path. Kepware exposes values; it does not by itself create your production dashboard.')+
'<p>DB addressing and data types: <a href="#kepware-address">PTC address reference</a>. Driver options: <a href="#kepware-driver">PTC driver help</a>. Bulk setup: <a href="#kepware-csv">PTC CSV help</a>.</p>')
topic('acceptance','Prove the readings before handoff','Normal cycles, boundary cases, quality and rollback.',
steps('Observe at least five normal cycles. Compare a stopwatch/trace against PreviousCycleSeconds and both between-cycle fields. Count exactly one completion per accepted cycle, never repeated increments from a sustained TRUE input.',
'Test or simulate startup with both candidate bits already high. No completion should appear. Test a second closing edge while already active, low-then-high robot permission and an abort. Held values must remain intact after an aborted observation.',
'In an isolated test instance, finish a cycle exactly at 17:00, 06:00, 06:30 and the top of an hour. Confirm new-period ownership, gap handling and night-shift identity through midnight. Do not move the production clock.',
'Check that partial flags distinguish first/uncertain periods. On an approved communication interruption, verify the dashboard shows bad/stale/invalid rather than presenting an old timer as current.',
'Record actual DB numbers, byte offsets, units, channel/device paths and the customer-approved display fields. Current/previous count tags are counts, not rates extrapolated to an hour.',
'Archive the final per-machine TIA project and Kepware configuration. Export this checklist\'s local progress file. For rollback, disable ObserverEnable and, if approved, remove only the new cyclic/startup additions; do not replace whole production programs with old archives.')+
'<p>The new source has no motion outputs. Nonetheless a compile error, wrong call location, scan-time load, existing name collision or inappropriate download can affect production. Complete the site review before enabling it.</p>')
# Task data: repeated independently, with clear gates and linked instructions.
common=[
('backup','Archive the correct current project and identify the live CPU','network','Match the machine, CPU and online address. Resolve online/offline differences before making edits.'),
('access','Confirm approved network access and commissioning window','first','Have the operator/site approve the change and rollback plan. No forced signals.'),
('config','Observe the live hydraulic and robot-interface selectors','find-code','Watch DB200.DBX38.0–38.3 and DB200.DBX49.3 without modifying them. Do not infer live configuration from old saved data.'),
('start','Verify the physical start event and its signal edge','find-code','Confirm the selected closing-command branch is active and matches the intended cycle start.'),
('finish','Verify robot-low then robot-clear/high through a real cycle','find-code','Confirm the end permission return coincides with clearing the pins. Record waveform/polarity, not just a name.'),
('qualify','Identify automatic, communication-valid and abort inputs','find-code','Record exact verified source signals. Leave the observer disabled until all inputs are meaningful.'),
('free','Reserve unused names/numbers for every new block','blocks','Search CSI_ names first. Record the actual global, instance, FB and helper block numbers.'),
('source','Generate the core and the correct clock adapter','scl','Use External sources. Cancel any overwrite warning. No existing OB is generated from a source.'),
('clock','Check PLC local date/time and clock-call status','clock','Verify local time matches the shift schedule. Do not set the live PLC clock for testing.'),
('retention','Configure and record retention for report/state data','retention','Counts, keys and completeness flags must remain consistent. Review any STOP/initialization requirement.'),
('layout','Compile CSI_Report and verify each exported byte offset','blocks','Use the actual TIA Offset column and enter the confirmed report DB number below.'),
('ob1','Add the reporting call after existing OB1 processing','call','Call every normal scan, outside jumps; map only read-only existing signals into CSI_IO.'),
('startup','Add the standard startup guard without replacing old code','call','Set CSI_IO.StartupPending in the applicable startup path; clear it after the cyclic monitor call.'),
('compile','Save and compile with no unresolved errors','scl','Review the clock syntax and target compatibility. The supplied source has not been TIA-compiled.'),
('preview','Review target, block list and load preview before download','download','STOP GATE: cancel unexpected STOP, hardware/F changes, deletions or widespread DB initialization.'),
('load','Perform the approved limited software download','download','Keep ObserverEnable 0. Verify diagnostics and online/offline match after the change.'),
('enable','Verify all inputs, then enable only the new observer','call','With site approval, modify CSI_Report.ObserverEnable to 1. Never force the original machine bits.'),
('cycles','Validate at least five completed cycles and held times','acceptance','One completion = one count. Check live gap timing and that held values survive the next cycle.'),
('boundaries','Prove shifts, hour/date rollover and startup in an isolated test','acceptance','Include the 06:00–06:30 gap, midnight, aborts, clock failure and startup-high signals.'),
('archive','Archive final project and record the approved tag map','acceptance','Do not mark this complete until the offline and running revisions have been reconciled.')]
M=[]
for name in ['MM4','MM5','MM6']:
 v16=name=='MM6'
 M.append(dict(id=name,title=name+' programming',version='TIA V16' if v16 else 'TIA V15',family='CPU 1507S F in archive; verify live' if v16 else 'WinLC / WinAC path; verify live CPU',start='DB98.DBX112.7',output='Q34.0' if v16 else 'Q44.0',end='DB94.DBX29.6' if v16 else 'DB94.DBX29.4',raw='I705.7' if v16 else 'I505.7',archive='PF1000825_20260909_0943.zap16' if v16 else 'PF1000723_F_20260909_0943.zap15',tasks=[dict(id=name+'-'+id,title=t,topic=topic,detail=d) for id,t,topic,d in common]))
K=[]
for name in ['MM4','MM5','MM6']:
 K.append(dict(id='K-'+name,title=name+' tags',tasks=[dict(id='K-'+name+'-'+i,title=t,topic='kepware',detail=d) for i,t,d in [
('backup','Back up server project; confirm driver and device','Use the existing working channel/device where appropriate. Record version/license and the actual PLC endpoint.'),
('access','Verify the approved S7 connection from the server','Check CPU model/rack/slot/TSAP/security with the installed driver. Do not use the Unsolicited driver by mistake.'),
('group','Create or select '+name+'.Production tag group','Do not duplicate another machine\'s IP or overwrite its device.'),
('map','Enter the verified report DB number and check offsets','Compare the generated address reference below with the compiled CSI_Report Offset column.'),
('test','Read SchemaVersion, Heartbeat and one REAL with Good quality','SchemaVersion must be 1. Heartbeat must change. Compare REAL seconds against TIA, not raw TIME interpretation.'),
('tags','Add read-only counts, held times and validity/quality fields','Include current/previous hour, both current/previous shifts, live/held between times and previous cycle.'),
('client','Verify exact item paths in the customer\'s dashboard/client','Configure seconds/minutes display and stale/partial indicators; Kepware itself is not the dashboard.'),
('save','Save server configuration and hand off verified readings','Record paths, units, scan policy and tested results. Export checklist progress separately.')]]))
D=dict(schema=F,instructions=I,machines=M,kepware=K,sources=[dict(id=a,title=b,url=c) for a,b,c in sources])
(R/'content.json').write_text(json.dumps(D,ensure_ascii=False,indent=2))
print('topics',len(I),'tasks',sum(len(x['tasks']) for x in M+K),'fields',len(F))
